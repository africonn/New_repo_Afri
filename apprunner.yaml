version: 1.0
runtime: python311
build:
  commands:
    build:
      - pip install -r requirements.txt -v 2>&1
run:
  command: uvicorn app:app --host 0.0.0.0 --port 8080
  network:
    port: 8080
    env: APP_PORT
